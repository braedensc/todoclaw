// task-crud.ts — authored by a dedicated scenario writer; see lifecycle-intent.ts / plan-rules.ts / recap-core.ts for the patterns.
import type { Scenario } from '../../lib/types.ts'

export const scenarios: Scenario[] = []
