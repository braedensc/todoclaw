// Placeholder for the free-canvas urgency × importance grid. The drag/tap-to-place
// canvas, quadrants, and clustering land in a later Stage 3 PR — this stub exists so
// the shell + tab nav are wired now and that PR only touches this file.
export function GridView() {
  return (
    <section aria-label="Grid" className="rounded-xl border border-border-strong bg-panel p-8">
      <p className="text-muted">Grid — coming soon.</p>
    </section>
  )
}
