# list

Priority-ranked list view: rows ordered by task score, urgency/importance sliders, inline
edit, due-date picker, quadrant badges. `ListView.tsx` is a placeholder; built in a later
Stage 3 PR.
