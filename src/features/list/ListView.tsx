// Placeholder for the priority-ranked list view (sliders, inline edit, quadrant badges).
// Built in a later Stage 3 PR — this stub keeps the shell wired so that PR only edits here.
export function ListView() {
  return (
    <section aria-label="List" className="rounded-xl border border-border-strong bg-panel p-8">
      <p className="text-muted">List — coming soon.</p>
    </section>
  )
}
