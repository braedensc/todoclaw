# habits

Daily habits with expandable subtasks/steps. Completion is tracked per local day in
`daily_state` (`habit_done`, `subtask_done`). `HabitsView.tsx` is a placeholder; built in a
later Stage 3 PR.
