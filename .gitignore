# Reference material — never published
planning/

# Secrets
.env
.env.local
.env*.local
*.pem
*.key

# Dependencies
node_modules/

# Build output
dist/

# Stray root deno.lock — the real one lives at supabase/functions/deno.lock;
# running `deno` from the repo root generates a bogus root-level lockfile.
/deno.lock

# Playwright (E2E) artifacts
test-results/
playwright-report/
playwright/.cache/

# Seeded auth session for the local golden E2E suite (never committed)
e2e/.auth/

# Maintainer-local tooling + notes tied to the gitignored planning/ reference data (not in a fresh
# clone). The committed demo seed (scripts/demo-seed/) is the shared replacement for the seed script.
CLAUDE.local.md
scripts/eisenclaw-seed/
scripts/legacy-ui.ts
scripts/legacy-ui-mock-plan.cjs

# OS
.DS_Store

# Claude Code runtime (local-only, not project config)
.claude/audit.log
.claude/settings.local.json
.claude/launch.json
.claude/worktrees/
.claude/.stop-pr-nag/
.claude/hooks/__pycache__/
